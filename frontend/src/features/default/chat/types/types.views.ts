/**
 * Extend the base ComponentTypes enum with Chat-specific components
 * using TypeScript's declaration merging
 */
export const ChatViewsTypes = {
  CHAT_CONVERSATION: 'ChatConversation'
} as const;